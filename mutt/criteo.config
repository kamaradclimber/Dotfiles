set from        = "g.seux@criteo.com"
set postponed   = "+Drafts"
set record      = "+Sent"

#Contact completion
set query_command="~/.dotfiles/mutt/ldap.pl `grep 'machine criteois.lan' -A 2 ~/.netrc | tail -n 1 | sed -e 's/.*password //'` '%s'"
